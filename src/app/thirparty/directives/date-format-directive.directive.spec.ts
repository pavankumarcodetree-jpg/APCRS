import { DateFormatDirectiveDirective } from './date-format-directive.directive';

describe('DateFormatDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DateFormatDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
