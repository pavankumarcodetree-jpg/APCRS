import { NumericDotOnlyDirective } from './numeric-dot-only.directive';

describe('NumericDotOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericDotOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
