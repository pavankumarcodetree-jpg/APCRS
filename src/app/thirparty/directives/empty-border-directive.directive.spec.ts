import { EmptyBorderDirectiveDirective } from './empty-border-directive.directive';

describe('EmptyBorderDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptyBorderDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
