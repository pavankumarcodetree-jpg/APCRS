import { NumericOnlyDirectiveDirective } from './numeric-only-directive.directive';

describe('NumericOnlyDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericOnlyDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
