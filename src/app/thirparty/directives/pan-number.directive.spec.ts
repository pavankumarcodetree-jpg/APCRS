import { PanNumberDirective } from './pan-number.directive';

describe('PanNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new PanNumberDirective();
    expect(directive).toBeTruthy();
  });
});
