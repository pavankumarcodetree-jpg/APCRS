import { PincodevalidationDirective } from './pincodevalidation.directive';

describe('PincodevalidationDirective', () => {
  it('should create an instance', () => {
    const directive = new PincodevalidationDirective();
    expect(directive).toBeTruthy();
  });
});
