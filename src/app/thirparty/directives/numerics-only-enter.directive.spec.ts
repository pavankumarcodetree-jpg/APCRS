import { NumericsOnlyEnterDirective } from './numerics-only-enter.directive';

describe('NumericsOnlyEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericsOnlyEnterDirective();
    expect(directive).toBeTruthy();
  });
});
