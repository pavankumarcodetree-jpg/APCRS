import { MobileFirstFiveNotAllowedDirectiveDirective } from './mobile-first-five-not-allowed-directive.directive';

describe('MobileFirstFiveNotAllowedDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileFirstFiveNotAllowedDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
